{
  "display_name": "Receipt",
  "collection_source": "movement-sessions",
  "collection_target": "receipts",
  "surfaces": [
    "fulfillment"
  ],
  "depends_on": {
    "components": [
      "base"
    ]
  },
  "fixtures": [
    {
      "slug": "lifecycle-with-loss",
      "label": "One product, four movement types — the 5-column Reference arm",
      "description": "The only fixture rendering the CONDITIONAL Reference column — the 5-column arm, where `checkout-no-reference` is the 4-column floor. `hasReference` is `items.some((i) => i.reference)` and the two fixtures cover its two answers; a template that hard-coded the column would pass on this one alone. It also pins `itemColumnCount = 4 + 1`, which `edgeColumn` derives the page's second alignment edge from — so a column-count regression moves the #details block as well as the grid.\n\n⭐ The only fixture reaching all FOUR movement types in one document — `prep`, `check_out`, `check_in`, `mark_lost` (movements 877, 879, 881, 882). That is what exercises `actionLabel`, the generic transform the template carries deliberately instead of a lookup table in `@cfs/core` (the manager's `MovementTimeline.tsx` writes the same `startCase(camelCase(t))`, so `check_in` must read \"Check In\" on both surfaces). A one- or two-type session leaves most of it unrendered.\n\n⚠️ THE TWO WRITERS DIFFER, WHICH IS WHY BOTH CAPTURES ARE KEPT. This is a `journal-migration` reconstruction: `created_by.name` is \"Journal Migration\" rather than a person, and the cutover wrote a sentence into `reference` on EVERY row. The live check-out path writes `reference: \"\"` — that is `checkout-no-reference`. The pair covers both the column arm and both writers; neither is a near-duplicate.\n\nIt is also the only fixture whose quantities deliberately do not reconcile line-to-line: 30 prepped and checked out, 25 checked back in, 5 marked lost. A receipt states what changed hands per movement rather than a net, so the partial return and the loss are two rows, not one adjustment — an edit that starts netting them goes red here.\n\nCaptured from prod session `83bf4176-a4d2-8646-8b1d-5652cf3b5d1c` (order 893, one product, 90 units over 4 rows). Single order, so `showOrderBanner` is FALSE here and the banner arm is not covered — it has no prod carrier and is hand-authored beside these."
    },
    {
      "slug": "checkout-no-reference",
      "label": "Nine products checked out — the 4-column floor, live writer",
      "description": "The 4-column FLOOR: `reference` is `\"\"` on all 18 rows, so `hasReference` is false and the grid is exactly Item Description / Action / Qty / Movement #. Its partner is `lifecycle-with-loss`, the 5-column arm; the two cover both answers of the one conditional this template has, and neither substitutes. The floor also pins the OTHER value of `edgeColumn` — `itemColumnCount` is 4 here and 5 there, so the page's second alignment edge sits in a different place in each, and #details is checked against both.\n\n⭐ It is the LIVE writer, where the partner is a migration reconstruction, and that is the second reason to keep both. `created_by.name` is a real operator (masked on capture), the checkout path wrote `reference: \"\"` rather than a sentence, and every `check_out` row carries a real `lines[].location` pair while every `prep` row carries `lines: []`. So this is the document that shows the ordinary shape production writes today; the partner shows what the journal cutover left behind, and that population does not grow.\n\nIt is also the family's only MULTI-PRODUCT and only long grid — 9 products, 18 rows, 280 units — so it is what puts the items table under enough vertical pressure to show row rhythm and header treatment, where the partner is 4 rows. Deliberately still one page: the page-break case belongs in its own fixture if one is ever wanted.\n\n⚠️ Single order (1007), so `showOrderBanner` is FALSE here too. No prod session spans orders — measured — so the banner and the `null`-organization arms have no capture available and are hand-authored beside these, each saying in its own description why it is synthetic.\n\nCaptured from prod session `b2b50277-377d-4585-b69c-6eaff89f7a73`, a `prep` + `check_out` pair per product on 2026-08-27."
    },
    {
      "slug": "cross-order-banner",
      "label": "Three group banners, one customer — SYNTHETIC",
      "description": "SYNTHETIC, and it has to be: measured 2026-09-08, NO prod movement session spans orders, so `showOrderBanner` (`groups.length > 1`) is false in every capture available and the banner half of the grid is rendered by nothing. Do not \"clean it up\" into a capture — there is nothing to capture. It is still the driving case for the surface it comes from: `POST /returns` accepts whatever a worker was handed back, so an item from order A returned beside order B is the design, not a data fault.\n\nFOUR things no capture reaches, in one document because they co-occur in the real shape:\n1. THE ORDER BANNER, with `it.sessions.sessionQuantity` per group — 18 / 14 / 5 against a document total of 37, so a banner summing the wrong rows is visible rather than plausible.\n2. THE `null`-ORDER GROUP, rendering \"No order\". A manual movement swept into a session is still something the customer was handed, so it is banner-ed rather than dropped.\n3. THE PLURAL in #details — \"Order Numbers:\", `orders.length > 1`. Every capture renders the singular.\n4. THE FILENAME's OTHER ARM. `render.filename` drops the order number when `orders.length !== 1`. That conditional had a fixture on neither side until this pair.\n\n⚠️ Not run through `applyPii` — there is no capture to run — so every masked leaf is written by hand from `@cfs/core/utils/fixture-pii`'s vocabularies: the organization from `FAKE_ORGANIZATIONS`, the worker from `FAKE_FIRST_NAMES` + `FAKE_LAST_NAMES`. Keep it that way; the fixture-PII guard has no carve-out for hand-built fixtures, deliberately. ⭐ Not advisory either — the first write of this fixture was refused a clean lint on `created_by.name`, an invented surname the masker could not produce, so check 6 reads the vocabularies rather than the shape.\n\nPartner: `anonymous-cross-customer` — the same cross-order shape with `organization: null`, so the diff between those two goldens is the letterhead's customer-name line and nothing else."
    },
    {
      "slug": "anonymous-cross-customer",
      "label": "A session spanning two customers — no name on the page — SYNTHETIC",
      "description": "The `organization: null` arm, and the only fixture where the letterhead prints NO customer name. SYNTHETIC, and necessarily so twice over: measured 2026-09-08, no prod session carries a null `organization`, and none spans orders either — and the two are the same fact, because a session is anonymous exactly WHEN it spans customers. `agreedOrganization` (`api-cloudrun/src/lib/movementSessionFold.ts`) composes the spanned orders' frozen chains and yields `null` when they disagree, so this shape cannot be captured without two customers' goods in one return.\n\n⭐ `null` here is an ANSWER, not missing data, which is why it needs a golden rather than a guard: `POST /returns` accepts whatever a worker was handed back, and a receipt must not print one customer's name on another's goods. The template passes `session.organization?.name ?? null` and the shared letterhead partial guards it — so the failure this freezes is the partial printing \"null\", an empty line, or the previous customer's name, none of which any other fixture could catch.\n\nIt is a deliberate NEAR-TWIN of `cross-order-banner`: same cross-order shape, same 4-column grid, same `check_in`-only types, minimal content. **The diff between their two goldens is the customer-name line and nothing else** — the same controlled-pair discipline the invoice family uses for its param states, and the reason neither is a duplicate of the other. Keep them in step: re-authoring one means re-authoring both, or the pair stops being a comparison.\n\n⚠️ Not run through `applyPii` — there is no capture to run — so the worker name is written by hand from `@cfs/core/utils/fixture-pii`'s `FAKE_FIRST_NAMES` + `FAKE_LAST_NAMES`, and there is no organization name to invent. The fixture-PII guard has no carve-out for hand-built fixtures, deliberately, and it does refuse names outside those vocabularies."
    }
  ],
  "render": {
    "margin_top": 0.5,
    "margin_bottom": 0.62,
    "margin_left": 0.5,
    "margin_right": 0.5,
    "filename": "Receipt<% if (it.doc.orders.length === 1) { %> - Order <%= it.doc.orders[0].number %><% } %> - <%= it.doc.date.slice(0, 10) %>",
    "footer": "partials/shared/footer.eta"
  }
}
