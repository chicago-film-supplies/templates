{
  "display_name": "Pick Sheet",
  "collection_source": "pick-sheets",
  "collection_target": "packing_lists",
  "surfaces": [
    "fulfillment"
  ],
  "depends_on": {
    "components": [
      "base"
    ]
  },
  "fixtures": [
    {
      "slug": "multi-order-destination",
      "label": "Two orders at one address — both directions, crew work",
      "description": "The family's FIRST fixture, and the case it exists for: a CROSS-ORDER sheet at ONE address in BOTH directions. Address `destination:x07P9fuCbGctZmB4zqhg:crew:both` — what `/pick-sheet` composes for a destination bucket, whose gate DEFAULTS to `crew`. Re-captured 2026-09-09.\n\nSix jobs; the first three separate this family from `packing-list`.\n\n1. THE NULL LEG — \"Both directions\", the arm `packing-list.eta` REFUSES: it accompanies ONE movement, and rendering both doubles every rental line.\n2. TWO BUCKET COLUMNS CARRYING VALUES — one column holding everything cannot show the seven-way partition.\n3. THE GATE LINE — \"Crew work\"; both legs are ours to deliver and collect. `packing-list` prints no gate.\n4. BOTH BANNER LEVELS, each on its own count: two orders and two differently-named legs at one address, under a scope line naming the ADDRESS.\n5. THE BLANK-CELL EXPLANATION, both shapes. A booking is aggregate per (order, product, destination), so products repeat across legs and components across the two Pop Up Tents. One occurrence carries the numbers, the rest read \"counted under …\". The naive `bookings.find(...)` renders identically and states one quantity twice.\n6. THE SERVICE EXCLUSION and the groups it empties. Two `service` lines are dropped, and each is the only member of a `Delivery` group, so both banners go too. The ONLY cover for either arm — the sibling has no service line.\n\n⭐ ONE organization, so the letterhead names it; the shared-stage arm where it must NOT (`3100 W Fillmore St`) is covered by nothing.\n\n⭐ ONE ADDRESS IS ONE STREET across the document, and this golden is where that is visible. `applyPii` seeded on field PATH until core#91 and drew different fakes for one `destinations/{uid}`.\n\n⚠️ `scope.name` holds `destination.address.full` verbatim and is masked as an address, not as a company — `DISCRIMINANT_CATEGORY` routes it on the sibling `kind`.\n\n⚠️ No serialized row: the Stock column is OFF on both fixtures."
    },
    {
      "slug": "split-breakdown-one-order",
      "label": "A line in two states at once — one order, both directions",
      "description": "The SPLIT BREAKDOWN — one line holding units in two custody states at once — the reason the seven columns exist. Address `order:V3hl7IoFE51tElz7c4fr:all:both` (prod order 1012) — the address `FulfillmentDetail`'s menu composes, the per-order printable form of that screen. Re-captured 2026-09-09.\n\n⚠️ The plan behind this family recorded that NO booking had a split, from 80 sampled. Sampling artifact: over all 7,123 dev bookings SIX do, and this is one. Do not re-derive the \"hand-build it\" conclusion.\n\nFive jobs, none reachable from its sibling.\n\n1. THE SPLIT — a booking whose units sit in `reserved` and `out` at once. Two populated cells on ONE row, with a Total read off the BOOKING rather than summed from them, so a partition that disagrees with its own total surfaces here.\n2. ITS OWNER, on the same booking. That product appears TWICE under two different groups, so one occurrence prints the split and a Total larger than its own line quantity, and the other prints \"counted under …\" with blank cells. Both halves of `owner_path`, one product, one page.\n3. THE `order` SCOPE — the degenerate one (api-cloudrun#821), whose scope line reads \"for\" rather than \"everything open at\".\n4. A SINGLE-ORDER SHEET — `showOrderBanner` and `showDestinationBanners` are both FALSE, so #details prints the order number where the sibling prints a count. That cell is the only place a single-order sheet names its order.\n5. KIT PARENTS WITH NO BOOKING — `stock_method: \"none\"`, so they render blank while their components carry the units. A different cause from the sibling's counted-under rows, and now the only no-booking blank anywhere — services are excluded.\n\n⭐ An `order` address names one order by construction, so unlike an organization- or destination-scoped sibling this fixture's shape cannot decay as work closes. Its CONTENTS still can: the split is live custody, so check the two populated bucket cells survive before blessing.\n\n⚠️ Gate `all`, so no gate line — the sibling holds that arm."
    }
  ],
  "render": {
    "margin_top": 0.5,
    "margin_bottom": 0.62,
    "margin_left": 0.5,
    "margin_right": 0.5,
    "filename": "Pick Sheet - <%= it.doc.scope.name %>",
    "footer": "partials/shared/footer.eta"
  }
}
