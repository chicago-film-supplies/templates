{
  "display_name": "Packing List",
  "collection_source": "pick-sheets",
  "collection_target": "packing_lists",
  "surfaces": [
    "fulfillment"
  ],
  "depends_on": {
    "components": [
      "base"
    ]
  },
  "fixtures": [
    {
      "slug": "multi-order-delivery",
      "label": "Three orders, one organization — the delivery leg",
      "description": "The family's FIRST fixture, covering what the family exists for: a CROSS-ORDER sheet. Address `organization:s7WXepzsnaTbaKF0Et1T:all:delivery`, which pins the `<kind>:<uid>:<gate>:<leg>` grammar (api-cloudrun#824) on its `organization` arm. Re-captured 2026-09-09: orders 970, 1013 and 1014 across three destinations, 423 units.\n\n🔴 THIS FAMILY'S SOURCE IS OPEN WORK, SO THE ADDRESS IS STABLE AND THE DOCUMENT IS NOT. A delivery sheet holds the bookings still awaiting delivery, so an order leaves it the moment its units go `out`. RE-POINTED here: the previous source (`organization:nTpq3YIY4cth5todd4PV`, orders 964 + 1020) had gone out and returned a ONE-order sheet, which is not this fixture's shape. Check the sheet is still cross-order before blessing its golden — a decayed source degrades silently into a document that renders fine and covers nothing.\n\n1. THE GROUP HEADINGS, across all three orders — what `buildPackingListForLeg` (core#80, beta.326) exists to keep. The bug it fixed was SILENT: `groupByDestination` strips dividers while `buildPackingList` walks for the last `group` row it passed, so composing them returned null for every heading. The grid rendered and the headings a picker navigates by were absent. Only a rendered sheet catches it.\n\n2. RENTAL AND SALE TOGETHER — the delivery leg's defining property, carried by order 1013's six `sale` lines beside its rentals. Delivery admits both; collection is rental only, because a sale does not come back. A collection-leg fixture is NOT a near-duplicate.\n\n3. A COMPLETE DOCUMENT. `order_count` is 3 and the fold returns 3 with `next_cursor: null`, so `orders.length === order_count` holds — the template REFUSES a short document.\n\n4. THREE DESTINATIONS under gate `all`, mixing customer-collecting and crew legs.\n\n⚠️ NOT small any more, and that claim is dropped rather than quietly kept: at 423 units this golden no longer reads in one view. Its job is the cross-order shape."
    },
    {
      "slug": "single-order-collection",
      "label": "The collection leg — rental only, components flattened",
      "description": "The COLLECTION leg, and the family's only fixture for it: before it, half of what this family renders was compared against nothing while `visual-diff` passed (templates#226). `PickSheet.leg` is DATA, not a render param, so no param state can reach this rendering. Partner: `multi-order-delivery`.\n\nAddress `order:YWzMc44iBt6bFWPrci0f:all:collection` — prod order 1007. ⭐ RE-POINTED 2026-09-09 from `organization:kmyrrrizBnD4J2ADzThK:all:collection`: an organization-scoped sheet holds whatever is open at that scope TODAY, and a second order had joined it, so that address no longer named a single-order sheet. The degenerate `order` scope (api-cloudrun#821) names one order by construction, so the defining property cannot decay away. Its sibling covers the `organization` arm.\n\nThree load-bearing branches, unrendered before it: `legKey` selects `packing_list_collection`; `legLabel` sets the heading to \"Packing List — Collection\"; the returns column is driven by `orderHasRentals`.\n\n⭐ RENTAL ONLY, not by chance — the leg's defining property. A sale does not come back; the delivery partner deliberately carries a `sale` line beside its rentals.\n\n⭐ IT PINS TWO INTENDED ABSENCES no other fixture reaches, its source being the only one with real COMPONENTS — five products hang off each of two Pop Up Tents, so `path` runs four deep.\n1. The sheet does NOT indent them; every row renders flush. `packing-list.css`'s `padding-left` rules are for the three BANNER levels and there is no per-depth row class, so an edit that starts indenting components goes red here.\n2. The same product renders on TWO ROWS rather than consolidating — `buildPackingList` keeps its expanded depth-first form.\n\nCOMPLETE: `order_count` is 1 and the fold returns 1 with `next_cursor: null`, so the `orders.length === order_count` assertion holds.\n\n⭐ Same prod order as the receipt family's `checkout-no-reference` — that is these tents going out, this is collecting them back. ⚠️ `scope.name` is filler here — core#99."
    }
  ],
  "render": {
    "margin_top": 0.5,
    "margin_bottom": 0.62,
    "margin_left": 0.5,
    "margin_right": 0.5,
    "filename": "Packing List<% if (it.doc.leg === 'collection') { %> - Collection<% } else { %> - Delivery<% } %> - <%= it.doc.scope.name %>",
    "footer": "partials/shared/footer.eta"
  }
}
