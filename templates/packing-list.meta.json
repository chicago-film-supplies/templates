{
  "display_name": "Packing List",
  "collection_source": "pick-sheets",
  "collection_target": "packing_lists",
  "surfaces": [
    "fulfillment"
  ],
  "depends_on": {
    "components": [
      "base"
    ]
  },
  "fixtures": [
    {
      "slug": "multi-order-delivery",
      "label": "Two orders, one organization — the delivery leg",
      "description": "The family's FIRST fixture, covering what the family exists for: a CROSS-ORDER sheet. Two orders (964, 1020) under one organization, two destinations, 13 units — no per-order document can express this. Scope `organization:...:all:delivery` pins the `<kind>:<uid>:<gate>:<leg>` grammar (api-cloudrun#824) on its `organization` arm.\n\n1. THE GROUP HEADINGS. Three group dividers across the two orders (`Carts & Ramps`, `Traffic, Safety & Signage`, `Crafty`) — what `buildPackingListForLeg` (core#80, beta.326) exists to keep. The bug it fixed was SILENT: `groupByDestination` strips dividers while `buildPackingList` walks for the last `group` row it passed, so composing them returned null for every heading. The grid rendered and the headings a picker navigates by were absent. Only a rendered sheet catches it.\n\n2. RENTAL AND SALE TOGETHER — the delivery leg's defining property. 964 is three rentals, 1020 a `sale`. Delivery admits both; collection is rental only, because a sale does not come back. A collection-leg fixture is NOT a near-duplicate.\n\n3. A COMPLETE DOCUMENT. `order_count` is 2 and the fold returns 2 with `next_cursor: null`, so `orders.length === order_count` holds — the template REFUSES a short document, and only a fixture satisfying it exercises that.\n\n4. IT PINS AN ABSENCE. All three of 964's bookings carry `shortage: 1` and one carries `stores: []`. The template renders NEITHER, deliberately — a shortage is resolved before the goods leave, not printed on paper that travels with them. An edit that starts printing shortages goes red here rather than reaching a picker.\n\nDeliberately SMALL so the golden reads in one view. The 17-order sheet at `3100 W Fillmore St` is the page-break case and needs its own fixture.\n\nRe-captured 2026-09-06 on the corrected masker (api-cloudrun#837): masked leaves now route by FIELD category, where they were person-name filler. Both orders mask alike because they ARE one destination and the salt is deterministic."
    },
    {
      "slug": "single-order-collection",
      "label": "The collection leg — rental only, components nested",
      "description": "The COLLECTION leg, and the family's first fixture for it: until this existed roughly half of what it renders had never been compared against anything, while `visual-diff` reported the family passing (templates#226). `PickSheet.leg` is the document's own field, not a render param — the `collection_leg` boolean the family was specified with was dropped because the leg split is DATA — so the two legs are two different renderings and no param state can reach this one. Partner: `multi-order-delivery`.\n\nThe branch is load-bearing in three places, all unrendered before this: `legKey` selects `packing_list_collection` over `packing_list_delivery`; `legLabel` changes the heading; the returns column is driven by `orderHasRentals` at this leg.\n\n⭐ RENTAL ONLY, and not by chance — that is the leg's defining property. All nine bookings are `type: rental`, because a sale does not come back; the delivery partner deliberately carries a `sale` line beside its rentals. Neither is a near-duplicate.\n\nIt also brings COMPONENT NESTING, which the delivery fixture lacks: the tie-downs, rain gutter, milk crate and both Tent Side/Door colours hang off the two Pop Up Tent LINE ITEMS rather than off the group divider, so `path` runs four deep and the per-level indent is exercised. Every other packing-list row bottoms out at a group.\n\nCOMPLETE: `order_count` is 1 and the fold returns 1 with `next_cursor: null`, so the template's `orders.length === order_count` assertion holds — it refuses a short document.\n\n⭐ Captured from the SAME prod order (1007) as the receipt family's `checkout-no-reference`: that fixture is these tents being checked out, this is the sheet for collecting them back.\n\nScope `organization:...:all:collection` — 140 units, one destination, `customer_collecting` and `customer_returning` both true, so a counter return rather than a truck.\n\n⚠️ KNOWN ARTIFACT, do not repair: `scope.name`, `orders[].subject` and `destinations[].name` render as masked filler."
    }
  ],
  "render": {
    "margin_top": 0.5,
    "margin_bottom": 0.62,
    "margin_left": 0.5,
    "margin_right": 0.5,
    "filename": "Packing List<% if (it.doc.leg === 'collection') { %> - Collection<% } else { %> - Delivery<% } %> - <%= it.doc.scope.name %>",
    "footer": "partials/shared/footer.eta"
  }
}
