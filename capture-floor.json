{
  "min_core": "10.0.0-beta.376",
  "why": "`aging-reports` became a template source in beta.376 (api-cloudrun#712's AR Aging family), and it brought three pii masks with it: `scope.name`, and the organization chain at `rows[].organization_path[].name` and `organizations[].organization_path[].name`. A build below this floor passes captureFloorVerdict and writes all three to git UNMASKED. ⭐ Unlike the beta.365 raise below it, the lint's own computed answer needed no manual correction this time, and the reason is worth keeping: those three leaves reuse `(parent, leaf)` pairs `statements` had ALREADY introduced — `scope.name` and `organization_path.name` — so `core/src/utils/fixture-pii.ts`'s QUALIFIED_CATEGORY needed no new route, and the TAG bar and the ROUTE bar therefore fall on the same version. That is why a new SOURCE is not automatically a new MASK, and why checking which of the two a change is costs one walk. Subsumes beta.365's statement router fix and every pii mask below it. ⚠️ Verified against the deployment DIFFERENTLY from every raise before it, and deliberately: prod serves revision api-cloudrun-00354-jqs from v0.238.0, whose deno.json pins 10.0.0-beta.374 — so this floor sits ABOVE the deployed core and refuses every capture until api-cloudrun releases beta.376. That is not an oversight and it costs nothing, because the PR carrying this file cannot merge before that release either: `deno task lint:deployed-enums` blocks it on the same three vocabulary members (`collection_source`/`collection_target` `aging-reports`, `surfaces` `report`), so both gates open at the same moment. Re-read the running pin before assuming that is still true.",
  "see": "api-cloudrun#838 — a capture is sanitized by the DEPLOYED api's @cfs/core, and no repository holds that fact. captureFixture reads THIS file from the base branch and refuses to sanitize with an older build. templates#213 — `deno task lint:capture-floor` detects a floor that has fallen BELOW the masks that exist; it deliberately does not object to one raised ABOVE them, which is what this floor is. templates#155 — why a floor raised above the deployed core is safe HERE specifically: the family PR that needs it is already blocked on the same release by the deployed-enums lint. api-cloudrun#712 — the Org Statement and the AR Aging report, whose template families are what make `statements` and `aging-reports` capturable sources. Raise min_core ONLY to a version api-cloudrun has DEPLOYED, or every capture is refused until it does — read the running pin off `/openapi.json`'s info.version, map it to a release tag, and read that tag's deno.json."
}
