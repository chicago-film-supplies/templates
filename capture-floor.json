{
  "min_core": "10.0.0-beta.386",
  "why": "core#91/#92/#93 landed together in beta.386 and moved BOTH bars at once, which is why this raise is not the usual shape. (1) A TAG raise: core#93 replaced the three light-shape `{uid, name}` org refs with `{uid, organization_path}`, so `pick-sheets:orders[].organization.organization_path[].name`, `pick-sheets:organizations[].organization_path[].name` and `movement-sessions:organization.organization_path[].name` are new masked leaves, and core#92 added `aging-reports:organization_path[].name` as the replacement for the deleted `AgingScope.name`. Five tags went the other way — the four `name`s those replaced, plus `statements:scope.name` — so the corpus is 195 -> 194 and a raise driven by the NET count would have read as a shrink. (2) A ROUTE raise that no tag change would have revealed: core#91 stopped seeding a fake on the FIELD PATH and started seeding it on the subject's identity, so one organization is one company name everywhere in a document and one address is one street. A build below this floor still masks every leaf and still passes the oracle — it simply produces the wrong KIND of fake, which is api-cloudrun#837's class. ⚠️ That half was UNDETECTABLE until this PR: `lint-capture-floor` computed staleness from the tag set alone and consulted routes only inside `covers()`, which runs after staleness is already established — so a route-only change printed a green `covers every pii tag` and let the floor rot. The trigger is armed now; templates#251 raised the bar of the ANSWER without ever arming the QUESTION. ⚠️ Verified against the deployment, not assumed: on 2026-09-09 both prod (api-cloudrun-00360-l2w) and dev serve v0.244.0, whose deno.json pins 10.0.0-beta.386 — so this floor is AT the deployed core rather than above it, and all twelve captures in this PR were taken against it. Re-read the running pin before assuming that is still true. ⭐ The floor deliberately did NOT move to beta.387: that beta repairs the mask ORACLE (`utils/template-lint.ts` dropped the `siblings` argument, so a correctly masked `scope.name` read as a leak), and an oracle is not a masker — nothing about what a capture STORES changed, which is the only question this floor asks. `deno task lint:capture-floor` agrees: 194 tagged leaves at .386, 194 at .387. Subsumes beta.376's aging-report raise and every mask below it.",
  "see": "api-cloudrun#838 — a capture is sanitized by the DEPLOYED api's @cfs/core, and no repository holds that fact. captureFixture reads THIS file from the base branch and refuses to sanitize with an older build. templates#213 — `deno task lint:capture-floor` detects a floor that has fallen BELOW the masks that exist; it deliberately does not object to one raised ABOVE them, which is what this floor is. templates#155 — why a floor raised above the deployed core is safe HERE specifically: the family PR that needs it is already blocked on the same release by the deployed-enums lint. api-cloudrun#712 — the Org Statement and the AR Aging report, whose template families are what make `statements` and `aging-reports` capturable sources. Raise min_core ONLY to a version api-cloudrun has DEPLOYED, or every capture is refused until it does — read the running pin off `/openapi.json`'s info.version, map it to a release tag, and read that tag's deno.json."
}
