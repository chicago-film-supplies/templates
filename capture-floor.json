{
  "min_core": "10.0.0-beta.355",
  "why": "the corrected fixture-PII ROUTER (beta.355), not a tag. `fakeForMask` chose a fake's category from the VALUE's shape until api-cloudrun#837, so it masked a venue as a person and a subject as a street address — fake, but the wrong kind of fake, and unverifiable by any oracle. beta.355 is where that routing table moved into `@cfs/core/utils/fixture-pii`, and api-cloudrun consumes it from the SAME commit that took the pin, so a build at or above this floor necessarily has the fix. Subsumes every pii mask below it, including invoices.notes (beta.351) and the four pick-sheet masks (beta.324-.328).",
  "see": "api-cloudrun#838 — a capture is sanitized by the DEPLOYED api's @cfs/core, and no repository holds that fact. captureFixture reads THIS file from the base branch and refuses to sanitize with an older build. templates#213 — `deno task lint:capture-floor` detects a floor that has fallen BELOW the masks that exist; it deliberately does not object to one raised ABOVE them, which is what this floor is. Raise min_core ONLY to a version api-cloudrun has DEPLOYED, or every capture is refused until it does — read the running pin off `/openapi.json`'s info.version, map it to a release tag, and read that tag's deno.json."
}
